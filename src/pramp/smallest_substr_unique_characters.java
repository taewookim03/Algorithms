package pramp;

public class smallest_substr_unique_characters {
    //given an array of unique characters and a string, find the smallest substring containing all characters
    //in the array
    /*
    e.g.
    unique characters = [x, y, z]
    str = xyyzyzyx
    ans: zyx

    O(n)
    solved by expanding right, then shrinking left. used a set and count map to check if substring contained
    all the characters using hash set/map.
     */


}
